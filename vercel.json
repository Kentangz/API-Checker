{
  "framework": "nextjs",
  "installCommand": "corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "corepack enable && pnpm --filter @api-key-checker/web build",
  "devCommand": "pnpm --filter @api-key-checker/web dev",
  "outputDirectory": "apps/web/.next"
}
